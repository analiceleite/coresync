import React from 'react';
import BaseContainer from '../../containers/global/container';

const Profile = () => {
    return(
        <BaseContainer>
        </BaseContainer>
    );
};

export default Profile;